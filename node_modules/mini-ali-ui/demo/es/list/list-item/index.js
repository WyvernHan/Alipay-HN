Component({
  props: {
    className: '',
    align: false,
    disabled: false,
    multipleLine: false,
    wrap: false,
    enforceExtra: false,
    titlePosition: 'top'
  },
  didMount: function didMount() {
    this._updateDataSet();

    var _this$props = this.props,
        _this$props$upperSubt = _this$props.upperSubtitle,
        upperSubtitle = _this$props$upperSubt === void 0 ? '' : _this$props$upperSubt,
        _this$props$lowerSubt = _this$props.lowerSubtitle,
        lowerSubtitle = _this$props$lowerSubt === void 0 ? '' : _this$props$lowerSubt;
    var thumbPlaceholder = '__' + upperSubtitle.slice(0, 1) + lowerSubtitle.slice(0, 1);
    this.setData({
      thumbPlaceholder: thumbPlaceholder
    });
  },
  didUpdate: function didUpdate() {
    this._updateDataSet();
  },
  methods: {
    _updateDataSet: function _updateDataSet() {
      this.dataset = {};

      for (var key in this.props) {
        if (/data-/gi.test(key)) {
          this.dataset[key.replace(/data-/gi, '')] = this.props[key];
        }
      }
    },
    onItemTap: function onItemTap(ev) {
      var _this$props2 = this.props,
          onClick = _this$props2.onClick,
          disabled = _this$props2.disabled;

      if (onClick && !disabled) {
        onClick({
          index: ev.target.dataset.index,
          target: {
            dataset: this.dataset
          }
        });
      }
    }
  }
});